{
  "data": {
    "val": "data/data_val.csv",
    "train": "data/data_train.csv"
  },
  "models": [
    {
      "batch_size": 128,
      "epochs": 10,
      "learning_rate": 0.01,
      "metrics": ["accuracy", "precision"],
      "layers": [
        {
          "size": 32,
          "activation": "relu"
        },
        {
          "size": 16,
          "activation": "relu"
        },
        {
          "size": 2,
          "activation": "softmax"
        }
      ]
    }
  ]
}
