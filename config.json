[
    {
        "batch_size": 32,
        "epochs": 500,
        "learning_rate": 0.001,
        "early_stopping_patience": 42,
        "restore_best_weights": true,
        "optimizer": "adam",
        "metrics": ["accuracy", "precision"],
        "hidden_layers": [
            { "size": 64, "initializer": "xavier", "activation": "sigmoid" },
            { "size": 32, "initializer": "he", "activation": "relu" },
            { "size": 16, "initializer": "he", "activation": "relu" }
        ]
    },
    {
        "batch_size": 16,
        "epochs": 20,
        "learning_rate": 0.001,
        "optimizer": "adam",
        "metrics": ["accuracy", "precision"],
        "hidden_layers": [
            { "size": 68, "initializer": "xavier", "activation": "sigmoid" },
            { "size": 32, "initializer": "he", "activation": "relu" },
            { "size": 16, "initializer": "he", "activation": "relu" }
        ]
    }
]
