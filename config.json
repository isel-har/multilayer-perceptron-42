[
    {
        "batch_size": 32,
        "epochs": 500,
        "learning_rate": 0.001,
        "early_stopping_patience": 40,
        "optimizer": "adam",
        "metrics": ["accuracy", "precision"],
        "hidden_layers": [
            { "size": 128, "initializer": "he", "activation": "relu"},
            { "size": 64, "initializer": "he", "activation": "relu" },
            { "size": 32, "initializer": "he", "activation": "relu" }
        ]
    }
]
