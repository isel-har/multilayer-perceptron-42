[
        {
        "batch_size": 16,
        "epochs": 25,
        "learning_rate": 0.001,
        "early_stopping_patience":5,
        "optimizer": "adam",
        "metrics": ["accuracy", "precision"],
        "hidden_layers": [
            { "size": 24, "initializer": "he", "activation": "relu" },
            { "size": 24, "initializer": "he", "activation": "relu" }
        ]
    },
    {
        "batch_size": 32,
        "epochs": 50,
        "learning_rate": 0.001,
        "early_stopping_patience":10,
        "optimizer": "adam",
        "metrics": ["accuracy", "precision"],
        "hidden_layers": [
            { "size": 64, "initializer": "he", "activation": "relu" },
            { "size": 32, "initializer": "he", "activation": "relu" },
            { "size": 16, "initializer": "he", "activation": "relu" }
        ]
    },
    {
        "batch_size": 32,
        "epochs": 200,
        "learning_rate": 0.001,
        "early_stopping_patience":15,
        "restore_best_weights":true,
        "optimizer": "adam",
        "metrics": ["accuracy", "precision"],
        "hidden_layers": [
            { "size": 64, "initializer": "he", "activation": "relu" },
            { "size": 32, "initializer": "he", "activation": "relu" },
            { "size": 16, "initializer": "he", "activation": "relu" }
        ]
    }
]
